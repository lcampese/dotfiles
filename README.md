# About
Unsurprisingly, this repository contains my configuration files.

# Installation
1. Clone this repository:

   ```sh
   git clone https://github.com/lcampese/dotfiles.git ~/.dotfiles
   ```

2. Run `install.sh` in the current shell:

   ```sh
   . .dotfiles/install.sh
   ```
